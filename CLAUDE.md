@AGENTS.md

Each package has its own AGENTS.md — read it before working in
that package. Repo skills live in `.claude/skills/`; prefer them
for PR preparation (`prepare-pr`) and behavior tests
(`write-integration-tests`).
